export enum UserRole {
  IT = "ROLE_IT",
  EC = "ROLE_EC",
}

export enum RequestState {
  DRAFT = "DRAFT",
  ONLINE = "ONLINE",
  CLOSED = "CLOSED",
  ARCHIVED = "ARCHIVED",
}

export enum ExecutionState {
  PENDING = "PENDING",
  PUBLISHED = "PUBLISHED",
  CLOSED = "CLOSED",
  ARCHIVED = "ARCHIVED",
}
